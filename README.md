# Frizeria din Volintiri

Minimal single-page site. Six sections: hero, about, services, prices, FAQ, contact.

```bash
npm install && npm run dev
```

Copy: [`messages/en.json`](messages/en.json). Phone appears in the hero and contact footer only (+ header on larger screens).
